1. map; flatMap 与 JDK中的Stream 相同

2. concatMap 与 flatMap 功能基本相同; 但是 concatMap 是将数据按照发射时的顺序聚合

3. window 按照窗口 进行数据聚合; 将满足窗口条件的一批数据聚合为一组数据  Flux<Flux<T>>

4. reduce 将上一个数据与当前数据进行处理,返回的新的数据; 当所有数据都处理完成,然后发布给订阅者; 可以设置初始化的第一个数据

5. scan  将上一个数据与当前数据进行处理,返回的新的数据; 将新的数据立即发送给订阅者
